overwrite: true
schema: "src/typeDefs/*.ts"
generates:
  src/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
      preResolveTypes: true
    hooks:
      afterOneFileWrite:
        - prettier --write