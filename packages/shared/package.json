{
  "name": "@monorepo/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared fonctions between @monorepo",
  "main": "src/index",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc",
    "lint": "prettier --write './src/**/*.ts'"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "nodemailer": "^6.4.10",
    "nodemailer-express-handlebars": "^4.0.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.0",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.7"
  }
}
